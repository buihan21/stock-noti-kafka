spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/alerts_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  main:
    allow-bean-definition-overriding: true
